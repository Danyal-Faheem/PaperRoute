# Benchmark data

`benchmark.json` is a public-paper benchmark fixture. It contains valid-format arXiv IDs, not downloaded papers. PDFs and runtime outputs are intentionally ignored. Do not add credentials, private papers, or personal data here. When pinning a new fixture, record the SHA-256 checksum and date in the manifest and changelog.
